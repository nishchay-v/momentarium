# Cloudflare R2 Configuration
# Required for publishing images to the gallery

# Your Cloudflare account ID (found in Cloudflare dashboard)
R2_ACCOUNT_ID=your_account_id

# R2 API token credentials (create in Cloudflare dashboard > R2 > Manage R2 API Tokens)
R2_ACCESS_KEY_ID=your_access_key_id
R2_SECRET_ACCESS_KEY=your_secret_access_key

# Name of your R2 bucket
R2_BUCKET_NAME=your_bucket_name

# Optional: Public URL for the bucket (if you have a custom domain or public bucket)
# This is used to generate public URLs for uploaded images
R2_PUBLIC_URL=https://your-bucket-domain.com
